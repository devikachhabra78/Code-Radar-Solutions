#include <stdio.h>
int main(){
    char ch;
    scanf("%c",&c);
    if(a >= 'a' && a <='z'){
        printf("Uppercase");
    }
    else{
        printf("Lowecase");

    }
    return 0;
}