#include <stdio.h>
int main(){
    char ch;
    scanf("%c",&c);
    if(isUpper(ch)){
        printf("Uppercase");
    }
    else if(isLower(ch)){
        printf("Lowercase");
    }
    return 0;
}